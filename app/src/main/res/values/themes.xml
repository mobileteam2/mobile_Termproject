<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 기본 앱 테마 -->
    <style name="Theme.Mobile_termproject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Add font -->
        <item name="android:fontFamily">@font/plus_jakarta_sans_bold</item>

        <!-- Primary brand color -->
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_light_primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_light_onPrimaryContainer</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/md_theme_light_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_light_onSecondary</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/md_theme_light_background</item>
        <item name="colorSurface">@color/md_theme_light_surface</item>
        <item name="colorOnSurface">@color/md_theme_light_onSurface</item>

        <!-- Status bar / Navigation bar -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorPrimary</item>

        <!-- 포커스 색상 지정 -->
        <item name="colorControlActivated">#369EFF</item>
        <item name="hintTextColor">#369EFF</item>
    </style>

    <!-- round -->
    <style name="FabPillShape" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerSize">50%</item>
    </style>
</resources>
